{
  "$id": "schema/min/lens_check.min.json",
  "allOf": [
    { "$ref": "../lens_check.json" },
    {
      "type": "object",
      "properties": {
        "assumption": { "type": "string", "minLength": 3, "maxLength": 256 },
        "method": { "type": "string", "enum": ["contrast","example","edge","proxy","other"] },
        "proxy": { "type": "string", "maxLength": 120 }
      },
      "required": ["assumption"],
      "additionalProperties": false
    }
  ]
}
